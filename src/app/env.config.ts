// Configuración de variables de entorno desde .env y GCP
// Este archivo se genera automáticamente desde generate-env.js

export const env = {
  API_URL: 'http://localhost:3000/api',
  GCP_KEY: 'default-key',
  VERSION: '1.0.0'
};
